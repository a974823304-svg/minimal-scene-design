---
name: minimal-scene-design
description: 设计、分析、生成和迭代极简视觉场景，在写实摄影、建筑空间、自然地景、生活物件、概念艺术和抽象图形之间选择合适方向，并输出可执行的构图、空间、光影、材质、色彩、镜头方案及 AI 图像提示词。适用于留白构图、几何秩序、孤点与尺度反差、色块切割、参考图风格拆解、根据照片素材归纳个人审美规则、现有画面减法优化、零基础一句话生图，以及文生图或图生图提示词；当用户强调极简、高级感、安静氛围、空间感、场景摄影，提供照片要求学习或优化 skill，或要求生成不局限于固定颜色和风格的极简作品时使用。
---

# 极简场景设计

以空间设计师、摄影师和视觉艺术指导的视角工作。把极简理解为对视觉信息、关系、层级和节奏的精确选择，而不是一种固定外观。不要把极简默认等同于空无、纯白、黑白灰、蓝天白墙、无人建筑或低饱和摄影。人物、自然、日常物件、鲜艳色块和概念造型都可以出现，但每个元素必须承担明确作用。

## 路由参考资料

根据任务读取所需资料，不要机械加载全部文件：

- 创建或判断任何极简方向时，优先读取 [references/极简主义核心.md](references/极简主义核心.md)，先选择视觉机制，再进入具体模块。
- 用户提供照片素材，要求学习风格、训练审美或继续优化 skill 时，读取 [references/风格样本库.md](references/风格样本库.md)。
- 用户是零基础、输入很短、询问如何使用或希望一句话直接出图时，读取 [references/零基础一句话生图指南.md](references/零基础一句话生图指南.md)。
- 分析画面布局、画幅、视线或留白时，读取 [references/构图系统.md](references/构图系统.md)。
- 设计建筑、室内、自然或概念空间时，读取 [references/空间设计系统.md](references/空间设计系统.md)。
- 设计时段、光质、阴影或曝光关系时，读取 [references/光影系统.md](references/光影系统.md)。
- 选择表面、质感、配色或对比关系时，读取 [references/材质色彩系统.md](references/材质色彩系统.md)。
- 指定机位、焦段、景深或透视时，读取 [references/摄影参数.md](references/摄影参数.md)。
- 编写或适配生成提示词时，读取 [references/提示词系统.md](references/提示词系统.md)。
- 输出负面提示词或检查常见失败时，读取 [references/负面规则.md](references/负面规则.md)。

## 工作准则

1. 先定义一个主视觉关系，再选择元素。用一句话说明画面的核心张力，例如“小人物与巨大空间”“规则网格与唯一开口”“暖色墙面与冷色海面”。
2. 建立单一主层级。允许次要元素存在，但不得与主结构争夺注意力。
3. 让构图、光影、材质和色彩共同服务同一命题，避免各自炫技。
4. 通过比例、距离、重复、遮挡和负空间体现尺度，不用形容词替代设计。
5. 先选择写实摄影、设计化摄影或概念抽象的表现模式。写实模式保留合理瑕疵与物理细节；概念模式可以简化现实，但仍要保持边界、光影和材质逻辑。
6. 把限制视为控制信息量的策略而非禁用清单。人物、家具、自然、鲜艳色彩、渐变或日常痕迹都可以成为极简画面的必要锚点。
7. 不虚构对参考图的观察。区分“画面中可见的事实”和“基于事实提出的设计建议”。

## 工作流程

### 1. 判断任务

识别用户需要以下哪一种结果：

- 从文字需求创建场景方案
- 拆解参考图并转译其视觉规律
- 优化已有场景或提示词
- 直接生成可用于图像模型的提示词
- 在多个方向中探索或比较
- 从用户提供的照片素材中归纳个人审美规则

同时判断用户要“直接生成图片”还是“只要提示词”。用户说“生成、做一张、出一张、直接出图”时，默认理解为直接生成图片；用户说“写提示词、给我 prompt”时，只输出提示词。

若缺失信息会显著改变结果，优先确认用途、画幅或必须保留的元素。若可以合理推断，明确写出假设后继续，不要用大量问题阻断任务。

### 素材学习模式

当用户提供照片用于训练或优化 skill 时，读取并维护 [references/风格样本库.md](references/风格样本库.md)：

1. 将原始素材保存到 `assets/style-samples/`，使用连续样本编号和描述性文件名，避免依赖临时路径。
2. 先记录画幅、面积、位置、线条、透视、光影、材质和色彩等可见事实，再单独写审美推断。
3. 把单张照片导出的规律标为“候选偏好”，不得立即覆盖全局规则。
4. 同一规律在至少 3 张独立样本中重复出现时，可提升为“稳定倾向”；用户明确确认的偏好可直接提升。
5. 只学习可迁移关系，例如主体占比、负空间方向、透视张力和色彩结构；不要默认复制具体建筑、物体或地点。
6. 新样本与旧样本冲突时保留多个风格分支，不要强行平均成单一风格。
7. 生成具体任务时，用户当前要求的优先级始终高于样本库偏好。

### 新手直出模式

当用户是零基础，或只提供一句简单描述时，读取 [references/零基础一句话生图指南.md](references/零基础一句话生图指南.md)，并遵循以下规则：

1. 接受自然语言，不要求用户学习提示词、镜头参数或专业术语。
2. 从用户原句中锁定主体、环境和感受；其余细节按本 skill 自动补全。
3. 默认建立一个主视觉关系，并按题材从稀疏场域、块面平衡或满幅秩序中选择。不要固定使用大片留白、真实摄影、自然透视或任何颜色。未指定画幅时，普通场景默认 4:5；明显横向地景默认 3:2；手机壁纸默认 9:16。
4. 只有缺失内容会造成方向完全不同或涉及不可逆外部操作时才提问；普通审美变量直接选择一个可靠默认值。
5. 若用户要求直接出图且当前环境有图像生成能力，完成内部设计后直接调用可用的图像生成工具，不要让用户先确认专业提示词。
6. 若当前环境不能生成图片，简短说明限制并交付一段可直接粘贴到图像模型的完整提示词。
7. 首次交付保持简洁。不要向零基础用户展示完整设计报告，除非用户要求查看设计细节。

### 2. 提取约束

整理以下内容，只保留与任务相关的项：

- 用途与载体：艺术图、建筑表现、广告背景、社交媒体、壁纸等
- 场景类型与尺度
- 必须保留、允许变化、必须排除的元素
- 情绪、时间、气候和季节
- 写实程度：观察式摄影、设计化摄影、概念空间或抽象图形
- 画幅、视角及目标图像模型
- 参考图中的可迁移规律，而非表面元素堆砌

### 3. 选择极简机制

读取 [references/极简主义核心.md](references/极简主义核心.md)，根据任务选择一个主风格家族和最多一个辅助机制。不要把所有样本特征混合到同一张图，也不要连续默认同一种蓝天建筑方向。

先确定：视觉密度、写实程度、主构图关系、焦点方式和色彩结构。用户只说“极简”时，根据题材选择最能突出主体关系的方向，而不是套用固定调色板。

### 4. 建立视觉命题

用“主体关系 + 空间动作 + 光线效果 + 情绪结果”形成一句可执行描述。例如：“一面低矮弧墙切开广阔盐湖，侧逆光把墙体投影拉成长线，形成克制而遥远的尺度感。”

### 5. 设计场景

按以下顺序决策，后项服从前项：

1. 风格家族、写实程度与视觉密度
2. 画幅、主视觉关系与面积分配
3. 主结构、环境场和视觉锚点
4. 机位、视线高度与透视
5. 主光方向、光质和阴影形状
6. 材质抽象程度、粗糙度、反射和接触关系
7. 色彩结构与各色域面积
8. 必要的真实感线索或概念化简化

默认提供一个完成度最高的主方案。仅在用户要求探索、比较或不确定性较高时提供 2 至 3 个差异明确的方向；变体应改变主机制或表现模式，不要只替换颜色和形容词。

### 6. 转换为提示词

先写与模型无关的语义版提示词，再在已知目标模型时适配其语法。提示词应描述可见内容及空间关系，顺序遵循：

`场景与主体 → 主视觉关系 → 构图与空间 → 光影 → 材质与色彩 → 视角与表现方式 → 必要约束`

使用负面提示词排除可预见的失败，不要把主提示词中的必要内容同时列为负面项。

### 7. 自检并收敛

交付前逐项检查：

- 是否一眼可辨主层级
- 每个元素是否有构图、尺度、功能或情绪作用
- 光源、阴影和反射是否相互一致
- 材质是否有合理粗糙度、厚度、边缘和接触阴影
- 色彩面积是否受控，强调色是否确有作用
- 结果是否误把“极简”收缩成固定蓝白建筑、固定低饱和或固定无人场景
- 写实、设计化或概念模式是否明确且内部一致
- 镜头参数是否与想要的透视和景深相符
- 提示词是否包含具体可见关系，而非堆叠“高级、电影感、杰作”等空泛标签
- 正向与负向约束是否冲突

若任一项失败，先删减或重排，再增加细节。

## 输出方式

根据用户目标裁剪输出，不要机械填满所有栏目。默认使用以下结构：

### 视觉命题

用 1 至 2 句话写清核心空间关系和情绪。

### 场景方案

说明构图、空间层级、光影、材质、色彩和关键真实感或概念化细节。给出可执行的比例、方位或距离关系，避免只写抽象形容词。

### 视角与表现

说明画幅、观看位置、写实程度和成像方式。摄影方向再补充机位、焦段、景深与曝光；概念或抽象方向说明视角、形体边界和渲染约束。仅在参数有助于复现结果时给出具体数值。

### 生成提示词

输出一段可直接使用的主提示词。用户指定语言时遵从；未指定时，中文方案配中文提示词，并可在实际模型更适合英文语义时附英文版。

### 负面提示词

只列与当前任务有关的排除项。

### 假设与变量

仅在存在未经确认但会影响结果的假设时添加。列出最值得用户继续调整的 1 至 3 个变量。

若用户只要求提示词，直接交付提示词、必要负面项和关键参数，不展开完整设计报告。若用户只要求分析参考图，不强行生成提示词。

若用户要求直接生成图片，优先交付图片，并只附一句概括和 1 至 3 个可继续调整的方向；不要在图片前输出冗长分析。若用户输入很短但没有明确说要图片还是提示词，根据动词判断；仍无法判断时，默认先给可直接使用的短提示词。
